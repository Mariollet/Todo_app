<li>
  <%= task.title %>
  <% unless task.deadline.nil? %>
    , to read: <%= task.deadline.strftime('%D')  %>
    <a href="tasks/<%= task.id %>" data-method="DELETE" data-remote="true"><i class="fas fa-trash-alt"></i></a>
  <% end %>
</li>